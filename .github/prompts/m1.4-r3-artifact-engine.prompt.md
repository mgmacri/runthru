---
description: "M1.4-R3: Create artifact classifier for tables, code blocks, figures — pure Dart, isolate-safe — plus unit tests"
agent: "agent"
model: ["Claude Sonnet 4 (copilot)", "GPT-4.1 (copilot)"]
---

# R3 — Artifact Detection Engine

> **Milestone**: M1.4 | **Epic**: E1.4.4 | **Feature**: F1.4.4.1
> **Tasks**: T1.4.4.1.1.1 + T1.4.4.1.1.2
> **Depends on**: nothing — parallel with R1, R2
> **Skills to load**: `flutter-handling-concurrency`, `flutter-testing-apps`

Load shared context: [M1.4 reading polish context](../instructions/m1.4-reading-polish-context.instructions.md)

---

## Task

Create a pure-Dart artifact classifier that detects tables, code blocks, figure captions, page markers, and bibliographic references in extracted document word lists. These artifacts degrade the RSVP experience and need to be flagged for display or skipping. The classifier must be **isolate-safe** — no Flutter imports, no FFI. Write comprehensive unit tests alongside the implementation.

## Files to Create

### 1. `lib/features/content/services/artifact_classifier.dart`

**Pure Dart — zero Flutter imports. Must be callable from `Isolate.run()`.**

```dart
/// Classifies regions of extracted text that are likely non-prose artifacts
/// (tables, code blocks, figure captions, page markers, references) which
/// degrade the RSVP reading experience.
///
/// Designed to run in an isolate — no Flutter or FFI dependencies.
library;

/// A detected artifact region within a word list.
class ArtifactRegion {
  /// Creates an artifact region spanning [startIndex] to [endIndex] inclusive.
  const ArtifactRegion({
    required this.startIndex,
    required this.endIndex,
    required this.type,
    required this.confidence,
  });

  /// Index of first word in the artifact (inclusive).
  final int startIndex;

  /// Index of last word in the artifact (inclusive).
  final int endIndex;

  /// The type of artifact detected.
  final ArtifactType type;

  /// Detection confidence from 0.0 (guess) to 1.0 (certain).
  final double confidence;

  /// Number of words in this region.
  int get length => endIndex - startIndex + 1;
}

/// Types of artifacts that can be detected.
enum ArtifactType {
  /// Tabular data — columns, delimiters, repeated numeric patterns.
  table,

  /// Code blocks — braces, semicolons, indentation patterns.
  codeBlock,

  /// Figure/table captions — "Figure 1:", "Table 2:", etc.
  caption,

  /// Page numbers and headers/footers.
  pageMarker,

  /// Bibliographic references — "[1]", "(Author, 2023)", etc.
  reference,
}
```

**Top-level classification function:**

```dart
/// Scan a word list and return detected artifact regions.
///
/// This function is isolate-safe — call via
/// `Isolate.run(() => classifyArtifacts(words))`.
List<ArtifactRegion> classifyArtifacts(List<String> words) {
  final regions = <ArtifactRegion>[];
  regions.addAll(_detectTables(words));
  regions.addAll(_detectCodeBlocks(words));
  regions.addAll(_detectCaptions(words));
  regions.addAll(_detectPageMarkers(words));
  regions.addAll(_detectReferences(words));
  return _mergeOverlapping(regions);
}
```

**Detection heuristics — implement these private functions:**

| Detector | Window | Signal Pattern | Threshold | Confidence |
|----------|--------|----------------|-----------|------------|
| `_detectTables` | 10 words sliding | Words matching `[0-9.\|_-]+`, pipe chars, dash-only, single chars | ≥6/10 match | `matchCount / windowSize` |
| `_detectCodeBlocks` | 15 words sliding | Contains `{`, `}`, `;`, `//`, `/*`, `->`, `=>`, `==`, `!=`, `>=`, `<=` | ≥6/15 code signals | 0.7 heuristic, 0.9 if `` ` `` markers | 
| `_detectCaptions` | word[i] matches pattern | Case-insensitive: `figure`, `fig.`, `table`, `chart` AND word[i+1] is a number | Match found | 0.85 |
| `_detectPageMarkers` | Single word | Isolated 1–4 digit number surrounded by non-numeric words, not adjacent to lowercase | Match found | 0.5 (low — may be real) |
| `_detectReferences` | 1–3 words | `[N]` where N is 1–3 digits, OR `(Author, YYYY)` pattern | Match found | 0.75 |

**Merging:**
```dart
List<ArtifactRegion> _mergeOverlapping(List<ArtifactRegion> regions) {
  // Sort by startIndex
  // Merge overlapping or adjacent regions (gap ≤ 2 words)
  // When merging, keep the higher-confidence type
}
```

**Critical constraint:** False positives on real prose are unacceptable. False negatives (missing an artifact) are tolerable. Err toward conservative detection.

### 2. `test/features/content/services/artifact_classifier_test.dart`

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:runthru/features/content/services/artifact_classifier.dart';

void main() {
  group('classifyArtifacts', () {
    test('returns empty list for plain prose', () {
      final words = 'The quick brown fox jumps over the lazy dog and runs away quickly today'.split(' ');
      expect(classifyArtifacts(words), isEmpty);
    });

    test('returns empty list for empty input', () {
      expect(classifyArtifacts([]), isEmpty);
    });

    test('detects pipe-delimited table content', () {
      final words = ['Name', '|', 'Age', '|', 'City', 'John', '|', '30', '|', 'NYC'];
      final regions = classifyArtifacts(words);
      expect(regions, isNotEmpty);
      expect(regions.first.type, ArtifactType.table);
    });

    test('detects code block with braces and semicolons', () {
      final words = ['void', 'main()', '{', 'print(', '"hello"', ');', '}'];
      final regions = classifyArtifacts(words);
      expect(regions, isNotEmpty);
      expect(regions.first.type, ArtifactType.codeBlock);
    });

    test('detects figure caption', () {
      final words = ['Figure', '1:', 'The', 'relationship', 'between', 'variables'];
      final regions = classifyArtifacts(words);
      expect(regions, isNotEmpty);
      expect(regions.first.type, ArtifactType.caption);
      expect(regions.first.startIndex, 0);
    });

    test('detects "Fig." caption variant', () {
      final words = ['See', 'Fig.', '3', 'for', 'details'];
      final regions = classifyArtifacts(words);
      final captions = regions.where((r) => r.type == ArtifactType.caption);
      expect(captions, isNotEmpty);
    });

    test('detects "Table" caption', () {
      final words = ['Table', '2:', 'Summary', 'of', 'results'];
      final regions = classifyArtifacts(words);
      expect(regions.first.type, ArtifactType.caption);
    });

    test('detects isolated page number', () {
      final words = ['end', 'of', 'chapter.', '42', 'The', 'next', 'chapter'];
      final regions = classifyArtifacts(words);
      final pageMarkers = regions.where((r) => r.type == ArtifactType.pageMarker);
      expect(pageMarkers, isNotEmpty);
      expect(pageMarkers.first.confidence, lessThan(0.6));
    });

    test('does not flag numbers within prose as page markers', () {
      final words = ['there', 'were', '42', 'people', 'in', 'the', 'room'];
      final regions = classifyArtifacts(words);
      final pageMarkers = regions.where((r) => r.type == ArtifactType.pageMarker);
      expect(pageMarkers, isEmpty);
    });

    test('detects bracketed references [N]', () {
      final words = ['as', 'shown', 'by', '[1]', 'and', '[2]', 'the'];
      final regions = classifyArtifacts(words);
      final refs = regions.where((r) => r.type == ArtifactType.reference);
      expect(refs, isNotEmpty);
    });

    test('merges adjacent table regions', () {
      final words = List.generate(20, (i) => i.isEven ? '|' : '$i');
      final regions = classifyArtifacts(words);
      expect(regions.length, lessThanOrEqualTo(2));
    });

    test('returns correct start/end indices for captions', () {
      final words = ['Hello', 'world.', 'Figure', '1:', 'Caption', 'text.', 'More', 'prose.'];
      final regions = classifyArtifacts(words);
      final caption = regions.firstWhere((r) => r.type == ArtifactType.caption);
      expect(caption.startIndex, 2); // "Figure"
      expect(caption.endIndex, greaterThanOrEqualTo(3)); // at least "1:"
    });

    test('does not false-positive on conversational prose', () {
      final words = 'I went to the store and bought some bread and milk for the family dinner tonight we had a great time'.split(' ');
      expect(classifyArtifacts(words), isEmpty);
    });
  });

  group('ArtifactRegion', () {
    test('length is calculated correctly', () {
      const region = ArtifactRegion(
        startIndex: 5,
        endIndex: 10,
        type: ArtifactType.table,
        confidence: 0.8,
      );
      expect(region.length, 6);
    });

    test('single-word region has length 1', () {
      const region = ArtifactRegion(
        startIndex: 3,
        endIndex: 3,
        type: ArtifactType.pageMarker,
        confidence: 0.5,
      );
      expect(region.length, 1);
    });
  });
}
```

---

## What NOT to Do

- Do NOT add Flutter imports to `artifact_classifier.dart` — it must be isolate-safe
- Do NOT modify `WordTimerNotifier` — suppression affects display, not timer logic
- Do NOT modify `lib/core/orp.dart`, `lib/three_d/`, or gesture code
- Do NOT over-engineer detection — these are heuristics, not ML
- Do NOT create a Riverpod provider for suppression state — that's R5's job
- Do NOT create the suppression overlay widget — that's R5's job
- Do NOT prioritize recall over precision — false positives on real prose are worse than missed artifacts

---

## Verification

```powershell
dart analyze lib/features/content/services/artifact_classifier.dart
flutter test test/features/content/services/artifact_classifier_test.dart
```

---

## Acceptance Criteria

- [ ] `ArtifactRegion` class with `startIndex`, `endIndex`, `type`, `confidence`, `length`
- [ ] `ArtifactType` enum: `table`, `codeBlock`, `caption`, `pageMarker`, `reference`
- [ ] `classifyArtifacts(List<String>)` → `List<ArtifactRegion>`
- [ ] Function is pure Dart — zero Flutter imports, isolate-safe
- [ ] Table detection: pipe-delimited and numeric-heavy patterns
- [ ] Code block detection: brace/semicolon/operator patterns
- [ ] Caption detection: "Figure N:", "Table N:", "Fig. N", "Chart N"
- [ ] Page marker detection: isolated numbers with low confidence (0.5)
- [ ] Reference detection: `[N]` patterns
- [ ] Overlapping/adjacent regions are merged
- [ ] Plain prose returns empty list — no false positives
- [ ] Numbers within prose ("42 people") are NOT flagged as page markers
- [ ] All tests pass
- [ ] `dart analyze` — zero issues
- [ ] All public symbols have dartdoc comments

---

## You May Refuse If

- `lib/features/content/services/` directory doesn't exist — create it
- An `artifact_classifier.dart` already exists — verify and extend

List the ambiguities instead of guessing.
